spring.application.name=Amaterasu-Pagos-Promotion-BackEnd
server.port=8080

# Configuración MongoDB
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=amaterasu_promotions

# Logging
logging.level.edu.eci.amaterasu.promotion=DEBUG

# Configuración de promociones
promotion.cleanup.cron=0 0 2 * * ?  # Ejecutar diariamente a las 2 AM
